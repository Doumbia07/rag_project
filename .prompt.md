Tu es un expert en Intelligence Artificielle et en Développement Backend.
Tu vas m'aider à finaliser la partie API de mon projet de moteur de recherche intelligent (RAG).

Contexte :
- Projet de moteur de recherche multi-domaines comparant 5 approches sur le corpus BEIR.
- Datasets : SciFact et NFCorpus, mais tous les moteurs doivent utiliser `scifact`.
- Moteurs : BM25, FAISS, Hybride, Reranking, RAG.
- Backend : Flask 2.3.3 + Flask-CORS 4.0.0.
- Python 3.11.
- Index FAISS pré-généré et métadonnées dans `models/`.

Objectif :
1. Générer le code complet pour tous les fichiers backend listés ci-dessous.
2. Fournir le contenu de `frontend/streamlit_app.py`, `requirements.txt`, `Dockerfile`, et `.gitignore`.
3. Donner des instructions précises pour lancer localement, tester avec `curl`, déployer sur Railway et connecter Base44.

Fichiers à produire :
- `backend/data_loader.py`
- `backend/bm25.py`
- `backend/faiss_search.py`
- `backend/hybrid.py`
- `backend/rerank.py`
- `backend/rag.py`
- `backend/evaluator.py`
- `backend/app.py`
- `frontend/streamlit_app.py`
- `requirements.txt`
- `Dockerfile`
- `.gitignore`

Contraintes techniques obligatoires :
- Python 3.11 uniquement.
- Tous les moteurs utilisent `scifact`.
- FAISS doit charger l'index depuis `models/`.
- Traduction automatique avec `deep-translator`, fallback dictionnaire si nécessaire.
- CORS activé.
- Réponses JSON uniquement.
- Gérer proprement les erreurs 404 / 500.
- Logs clairs en console avec emojis.
- Endpoint racine `/` et endpoints POST spécifiques.

API attendue :
- `GET /` : informations, endpoints, statut.
- `POST /search_bm25` : `{